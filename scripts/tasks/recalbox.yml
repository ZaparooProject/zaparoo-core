version: "3"

tasks:
  build-amd64:
    cmds:
      - task: :zigcc:build-linux-amd64
        vars:
          PLATFORM: recalbox

  build-arm64:
    cmds:
      - task: :zigcc:build-linux-arm64
        vars:
          PLATFORM: recalbox