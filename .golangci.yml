run:
  go: '1.23'

linters:
  enable:
    # Security & Correctness
    - staticcheck
    - errcheck
    - gosec
    - govet
    - unused
    - gosimple
    - ineffassign
    # Database specific
    - sqlclosecheck
    - rowserrcheck
    # Style & Quality
    - goimports
    - revive
    - gocritic
    - nolintlint
    # Complexity
    - gocyclo
    - funlen
    # HTTP
    - bodyclose
    # Code Standards
    - gochecknoglobals

linters-settings:
  govet:
    enable:
      - shadow
  gocritic:
    enabled-tags:
      - diagnostic
      - style
      - performance
  gocyclo:
    min-complexity: 15
  funlen:
    lines: 80
    statements: 50
  goimports:
    local-prefixes: github.com/ZaparooProject/zaparoo-core
